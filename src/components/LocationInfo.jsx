import styled from "styled-components";

const StyledLocationInfo = styled.div``;
const StyledLocationInfoItem = styled.div``;

function LocationInfo({ info }) {
    return <StyledLocationInfo></StyledLocationInfo>;
}

function LocationInfoItem({ obj }) {
    return <StyledLocationInfoItem></StyledLocationInfoItem>;
}

export default LocationInfo;
